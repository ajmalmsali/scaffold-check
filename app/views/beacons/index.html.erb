<h1>Listing beacons</h1>

<table>
  <thead>
    <tr>
      <th>Identifier</th>
      <th>Prox min</th>
      <th>Prox max</th>
      <th>Prox med</th>
      <th>Building</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @beacons.each do |beacon| %>
      <tr>
        <td><%= beacon.identifier %></td>
        <td><%= beacon.prox_min %></td>
        <td><%= beacon.prox_max %></td>
        <td><%= beacon.prox_med %></td>
        <td><%= beacon.building %></td>
        <td><%= link_to 'Show', beacon %></td>
        <td><%= link_to 'Edit', edit_beacon_path(beacon) %></td>
        <td><%= link_to 'Destroy', beacon, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Beacon', new_beacon_path %>
